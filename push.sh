git config --global user.email "seydiahmedelcheikh@gmail.com"
git config --global user.name "seydi-ahmed"
git config credential.helper store
git add .
git commit -m "
1) front et back opérationnel à 100%
2) reste le css
"
git push --force